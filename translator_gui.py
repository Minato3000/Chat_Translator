from tkinter import *
from tkinter import ttk

window = Tk()


textfield = Label(window, text='Chat')
textfield.pack()

next_button = Button(window, text='Next')
next_button.pack()

window.mainloop()
